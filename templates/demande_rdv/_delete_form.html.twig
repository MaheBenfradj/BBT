<form method="post" action="{{ path('app_demande_r_d_v_delete', {'id': demande_r_d_v.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_r_d_v.id) }}">
    <button class="btn">Delete</button>
</form>
